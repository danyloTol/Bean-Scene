import heroBg from '../assets/coffee_image.jpg'

export default function Hero() {
    return (
        <section style={{ backgroundImage: `url(${heroBg})`}} className='min-h-screen w-screen bg-cover '>

        </section>
    )
}